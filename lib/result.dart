import 'package:flutter/material.dart';
import 'C_TextWidget.dart';

class Result extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: C_TextWidget('You are done'),
    );
  }
}
